# Retron系统挖掘流程 v2.0 - 完整运行方案

## 目录结构

```
/home/teng/claude_code/retron/pipeline_v2/
├── config.yaml                    # 配置文件
├── main_pipeline.py               # 主控脚本
├── README.md                      # 说明文档
├── step01_search.py               # RT同源物搜索 (含位置去重)
├── step02_motif_filter.py         # Motif过滤 (NAXXH/VTG)
├── step03_classify_rt.py          # RT分类与排除
├── step04_extract_flanking.py     # 上游序列提取
├── step05_rna_filter.py           # RNA结构筛选
├── step06_extract_neighbors.py    # 邻近基因提取 (按基因簇输出)
├── step07_defense_island.py       # 防御岛分析 (基于基因簇)
├── step08_effector_detect.py      # 效应蛋白检测
├── step09_final_report.py         # 综合报告生成
└── run_defensefinder_batch.py     # DefenseFinder批量运行脚本 (单独环境用)
```

---

## 一、环境准备

### 1.1 依赖检查

```bash
# Python包
pip install biopython pandas pyyaml tqdm psutil intervaltree

# 检查外部工具
which diamond    # DIAMOND搜索
which RNAfold    # ViennaRNA

# 如果缺少，安装：
conda install -c bioconda diamond
conda install -c bioconda viennarna
```

### 1.2 可选工具（需要单独环境）

```bash
# DefenseFinder (防御岛分析)
conda create -n defensefinder python=3.9
conda activate defensefinder
conda install -c bioconda defense-finder
defense-finder update  # 更新模型

# InterProScan (效应蛋白检测)
conda create -n interproscan python=3.9
conda activate interproscan
conda install -c bioconda interproscan
```

### 1.3 数据准备

确保你有以下数据：

| 数据类型 | 说明 | 示例路径 |
|---------|------|---------|
| RT参考序列 | 已知Retron RT的fasta文件 | `/path/to/reference/*.faa` |
| 蛋白序列 | 3000个黏细菌基因组的.faa文件 | `/path/to/faa/*.faa` |
| 基因组序列 | 对应的.fasta文件 | `/path/to/fasta/*.fasta` |
| antiSMASH结果 | 用于提取邻近基因注释 | `/path/to/antismash/` |

---

## 二、配置文件设置

### 2.1 编辑配置文件

```bash
cd /home/teng/claude_code/retron/pipeline_v2
cp config.yaml my_config.yaml
vim my_config.yaml
```

### 2.2 关键配置项

```yaml
# ===== 必须修改的路径 =====
paths:
  query_dir: /你的路径/reference          # RT参考序列
  faa_dir: /你的路径/faa_files            # 蛋白序列目录
  fasta_dir: /你的路径/fasta_files        # 基因组序列目录
  antismash_dir: /你的路径/antismash      # antiSMASH结果
  output_dir: /你的路径/output            # 输出目录

# ===== 可选调整的参数 =====
search:
  min_identity: 25    # 降低可发现更多远缘RT，但假阳性增加
  min_coverage: 40

rna_filter:
  min_mfe: -15.0      # 高GC生物可适当放宽到-12
  require_branch_g: false  # 初筛设false，精筛可改true

pipeline:
  start_step: 1       # 起始步骤
  end_step: 9         # 结束步骤
```

---

## 三、运行方案

### 方案A：完整流程运行（推荐小样本测试）

```bash
cd /home/teng/claude_code/retron/pipeline_v2

# 1. 预览命令（不实际执行）
python main_pipeline.py -c my_config.yaml --dry-run

# 2. 实际运行（跳过DefenseFinder，后续单独处理）
python main_pipeline.py -c my_config.yaml --skip-defense

# 3. 或者使用已有的DefenseFinder结果
python main_pipeline.py -c my_config.yaml --df-results /path/to/df_results
```

### 方案B：跳过Step 1（使用已有搜索结果）

如果你已经有了搜索结果（如之前的complete_results.tsv）：

```bash
python main_pipeline.py \
  --search-results /path/to/existing/complete_results.tsv \
  --faa-dir /path/to/faa \
  --fasta-dir /path/to/fasta \
  --antismash-dir /path/to/antismash \
  --output ./retron_v2_output \
  --start-step 2 \
  --skip-defense
```

### 方案C：分步运行（调试用）

```bash
# Step 2: Motif过滤
python step02_motif_filter.py \
  -i /path/to/complete_results.tsv \
  -f /path/to/faa \
  -o ./02_motif_filtered

# Step 3: RT分类
python step03_classify_rt.py \
  -i ./02_motif_filtered/motif_filtered.tsv \
  -o ./03_classified

# Step 4: 上游序列提取
python step04_extract_flanking.py \
  -i ./03_classified/retron_candidates.tsv \
  -f /path/to/fasta \
  -o ./04_flanking

# Step 5: RNA结构筛选
python step05_rna_filter.py \
  -i ./04_flanking/flanking_sequences.fasta \
  -o ./05_rna_filtered

# Step 6: 邻近基因提取 (输出按基因簇组织)
python step06_extract_neighbors.py \
  -i ./05_rna_filtered/filter_results.tsv \
  -a /path/to/antismash \
  -o ./06_neighbors

# Step 7: 防御岛分析 (见下方详细说明)

# Step 8: 效应蛋白检测
python step08_effector_detect.py \
  -i ./05_rna_filtered/filter_results.tsv \
  -n ./06_neighbors/neighbors_info.tsv \
  -o ./08_effector

# Step 9: 综合报告
python step09_final_report.py \
  -o ./09_report \
  --motif ./02_motif_filtered/motif_filtered.tsv \
  --classified ./03_classified/retron_candidates.tsv \
  --rna ./05_rna_filtered/filter_results.tsv \
  --neighbors ./06_neighbors/neighbors_info.tsv \
  --defense ./07_defense_island/defense_analysis.tsv \
  --effector ./08_effector/effector_analysis.tsv
```

---

## 四、DefenseFinder 单独环境运行 (Step 7)

由于DefenseFinder需要在单独的conda环境中运行，提供以下两种方案：

### 4.1 方案一：两步运行（推荐）

```bash
# ===== Step 1: 在DefenseFinder环境中批量运行 =====
conda activate defensefinder

# 运行批量分析脚本
python run_defensefinder_batch.py \
  -i ./06_neighbors/clusters \
  -o ./defensefinder_results \
  -j 4  # 并行任务数

# ===== Step 2: 回到主环境整合结果 =====
conda activate main_env

# 使用已有结果运行step07
python step07_defense_island.py \
  -i ./06_neighbors \
  --df-results ./defensefinder_results \
  -o ./07_defense_island
```

### 4.2 方案二：直接在DefenseFinder环境运行

如果你在DefenseFinder环境中也安装了必要的Python包：

```bash
conda activate defensefinder
pip install pandas  # 确保有pandas

# 直接运行step07

```python step07_defense_island.py \
  -i ./06_neighbors \
  -o ./07_defense_island

### 4.3 在main_pipeline中使用已有结果

```bash
# 先运行Step 1-6
python main_pipeline.py -c my_config.yaml --end-step 6

# 在DefenseFinder环境中运行批量分析
conda activate defensefinder
python run_defensefinder_batch.py -i ./output/06_neighbors/clusters -o ./df_results

# 回到主环境，使用已有结果继续运行
conda activate main_env
python main_pipeline.py -c my_config.yaml --start-step 7 --df-results ./df_results
```

---

## 五、InterProScan 单独环境运行 (Step 8)

### 5.1 分段运行方案

```bash
# ===== 在主环境中运行 Step 1-7 =====
cd /home/teng/claude_code/retron/pipeline_v2

# 运行到Step 7为止
python main_pipeline.py -c config.yaml --end-step 7 --skip-defense

# ===== 切换到InterProScan环境运行 Step 8 =====
conda activate interproscan

# 方式A: 直接运行InterProScan + 分析
python step08_effector_detect.py \
  -i ./output/05_rna_filtered/filter_results.tsv \
  -n ./output/06_neighbors/neighbors_info.tsv \
  -p ./output/06_neighbors/clusters/*/neighbor_proteins.fasta \
  --use-interproscan \
  --cpu 8 \
  -o ./output/08_effector


# 方式B: 先单独运行InterProScan，再分析
# 合并所有基因簇的蛋白序列
cat ./output/06_neighbors/clusters/*/neighbor_proteins.fasta > all_neighbors.fasta

interproscan.sh \
      -i all_neighbors_dedup.fasta \
      -f tsv \
      -o ./retron_v2_results/08_effector/interproscan_results.tsv \
      -cpu 8 \
      -goterms -pa \
      -appl Pfam,TIGRFAM,Gene3D,SUPERFAMILY,PANTHER,SMART,ProSiteProfiles,ProSitePatterns,PRINTS,PIRSF,Hamap,MobiDBLite,Coils


# 然后可以回到主环境使用结果
conda activate main_env
python step08_effector_detect.py \
  -i ./output/05_rna_filtered/filter_results.tsv \
  -n ./output/06_neighbors/neighbors_info.tsv \
  --interproscan-results ./output/08_effector/interproscan_results.tsv \
  -o ./output/08_effector

# ===== 继续运行 Step 9 =====
python main_pipeline.py -c config.yaml --start-step 9
```

### 5.2 Step 8 的三种运行模式

| 模式 | 命令 | 说明 |
|------|------|------|
| **仅关键词** | `python step08_effector_detect.py -i ... -n ...` | 快速，不需要InterProScan |
| **运行InterProScan** | `... --use-interproscan -p proteins.fasta` | 需要InterProScan环境 |
| **使用已有结果** | `... --interproscan-results results.tsv` | 先跑interproscan.sh |

---

## 六、输出文件说明

```
output/
├── 01_search/
│   └── reports/complete_results.tsv    # 原始搜索结果 (已去重)
├── 02_motif_filtered/
│   ├── motif_filtered.tsv              # ★ Motif过滤后的候选
│   └── high_confidence.tsv             # NAXXH+VTG都有的RT
├── 03_classified/
│   ├── retron_candidates.tsv           # ★ Retron候选（排除了Group II等）
│   └── excluded_rt.tsv                 # 被排除的RT
├── 04_flanking/
│   └── flanking_sequences.fasta        # 上游序列
├── 05_rna_filtered/
│   ├── filter_results.tsv              # ★ RNA筛选结果
│   └── passed_candidates.fasta         # 通过筛选的序列
├── 06_neighbors/
│   ├── cluster_summary.tsv             # ★ 基因簇汇总
│   ├── neighbors_info.tsv              # 所有邻近基因信息
│   └── clusters/                       # 按基因簇组织的目录
│       └── {genome}_{contig}_{start}-{end}/
│           ├── neighbor_proteins.fasta # 邻近蛋白序列
│           ├── neighbors_info.tsv      # 该基因簇的邻近基因
│           └── metadata.txt            # 元数据
├── 07_defense_island/
│   ├── defense_analysis.tsv            # ★ 防御系统分析结果
│   ├── defense_genes_detail.tsv        # 防御基因详情
│   └── clusters_with_defense.tsv       # 含防御系统的基因簇
├── 08_effector/
│   └── effector_analysis.tsv           # 效应蛋白检测结果
└── 09_report/
    ├── final_results.tsv               # ★★ 最终结果（含置信度评分）
    ├── high_confidence_candidates.tsv  # ★★ 高置信度候选
    ├── medium_confidence_candidates.tsv
    └── summary_report.md               # 摘要报告
```

---

## 七、小样本测试建议

### 7.1 选择测试基因组

建议选择50-100个基因组进行初步测试：

```bash
# 创建测试子集目录
mkdir -p test_data/{faa,fasta}

# 选择一些已知的黏细菌基因组
# 必须包含: Myxococcus xanthus (含有已知的Mx162, Mx65 Retron)
cp /path/to/faa/GCF_000012685*.faa test_data/faa/  # M. xanthus DK1622
cp /path/to/fasta/GCF_000012685*.fasta test_data/fasta/

# 添加其他代表性基因组...
```

### 7.2 测试运行

```bash
# 修改配置指向测试数据
cp my_config.yaml test_config.yaml
vim test_config.yaml  # 修改路径指向test_data

# 运行测试（跳过DefenseFinder）
python main_pipeline.py -c test_config.yaml -v --skip-defense

# 检查结果
cat output/09_report/summary_report.md
```

### 7.3 验证检查点

测试完成后，检查以下关键结果：

1. **Step 1 (搜索)**：检查去重是否生效（日志中会显示去重数量）
2. **Step 2 (Motif过滤)**：高置信度(NAXXH+VTG)应该大幅减少
3. **Step 5 (RNA筛选)**：应该能检出M. xanthus中的已知Retron ncRNA结构
4. **Step 6 (邻近基因)**：检查clusters目录结构是否正确
5. **Step 9 (报告)**：高置信度候选应包含已知Retron

---

## 八、常见问题

### Q1: Step 7 DefenseFinder运行很慢怎么办？

```bash
# 方案1: 跳过防御岛分析
python main_pipeline.py -c my_config.yaml --skip-defense

# 方案2: 使用并行批量运行
conda activate defensefinder
python run_defensefinder_batch.py -i ./06_neighbors/clusters -o ./df_results -j 8
```

### Q2: 如何调整茎长度阈值？

RNA筛选不使用茎长作为硬过滤条件，而是在最终评分时考虑。如果需要更严格的筛选：

```bash
# 在Step 5后手动过滤
python -c "
import pandas as pd
df = pd.read_csv('05_rna_filtered/filter_results.tsv', sep='\t')
df_filtered = df[df['longest_stem'] >= 15]
df_filtered.to_csv('05_rna_filtered/filter_results_stem15.tsv', sep='\t', index=False)
"
```

### Q3: 如何只看高置信度结果？

```bash
cat output/09_report/high_confidence_candidates.tsv
# 或
cat output/09_report/summary_report.md
```

### Q4: 搜索结果中有重复怎么办？

Step 1 现在会自动进行位置去重。如果使用已有的搜索结果，可以手动去重：

```bash
python -c "
import pandas as pd
df = pd.read_csv('complete_results.tsv', sep='\t')
df = df.sort_values('identity', ascending=False)
df = df.drop_duplicates(subset=['genome', 'matched_contig', 'genome_nt_start', 'genome_nt_end'], keep='first')
df.to_csv('complete_results_dedup.tsv', sep='\t', index=False)
print(f'去重后: {len(df)} 条')
"
```

---

## 九、后续分析建议

1. **高置信度候选**：直接进行实验验证（msDNA检测、噬菌体攻毒实验）
2. **中等置信度**：补充HHpred结构域分析，或构建CM模型扩展搜索
3. **系统发育分析**：对高置信度RT构建进化树，分析Retron类型分布
4. **效应蛋白功能预测**：对检出的效应蛋白进行更深入的功能域分析
