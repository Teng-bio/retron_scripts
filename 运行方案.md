# Retron系统挖掘流程 v2.0 - 完整运行方案

## 方法论说明

本流程基于 **Mestre et al., 2020 NAR** 的统计关联分析方法：

| 方法 | 核心思路 | 工具依赖 |
|------|---------|---------|
| **Mestre (本流程)** | RT → 提取±30kb内所有ORF → MMseqs2聚类 → Phyvalue统计 → 类型分类 | MMseqs2 |
| Millman (旧方法) | RT → 检测防御岛 → DefenseFinder验证 | DefenseFinder |

**优势**：无监督聚类可发现新型关联蛋白，不依赖已知防御系统数据库。

---

## 目录结构

```
/home/teng/claude_code/retron/pipeline_v2/retron_scripts/
├── main_pipeline.py               # 主控脚本
├── step01_search.py               # RT同源物搜索 (含位置去重)
├── step02_motif_filter.py         # Motif过滤 (NAXXH/VTG)
├── step03_classify_rt.py          # RT分类与排除
├── step04_extract_flanking.py     # 上游序列提取
├── step05_rna_filter.py           # RNA结构筛选
├── step06_extract_neighbors.py    # ★ 全量邻近蛋白提取 (±30kb, Mestre方法)
├── step07_mmseqs_cluster.py       # ★ MMseqs2聚类与共现矩阵 (Mestre方法)
├── step08_phyvalue_analysis.py    # ★ Phyvalue统计关联分析 (Mestre方法)
├── step09_final_report.py         # ★ Retron类型分类与报告 (Type I-XIII)
├── 运行方案.md                     # 本文档
│
├── [已弃用] step07_defense_island.py   # 旧版：DefenseFinder方法
└── [已弃用] step08_effector_detect.py  # 旧版：InterProScan方法
```

---

## 一、环境准备

### 1.1 主环境依赖

```bash
# Python包
pip install biopython pandas numpy pyyaml tqdm psutil

# 检查外部工具
which diamond    # DIAMOND搜索
which RNAfold    # ViennaRNA RNA折叠
```

### 1.2 MMseqs2 单独环境 (必须)

由于MMseqs2环境配置问题，需要在单独的conda环境中运行：

```bash
# 创建MMseqs2环境
conda create -n mmseqs_env python=3.9
conda activate mmseqs_env
conda install -c bioconda mmseqs2

# 验证安装
mmseqs --help
```

### 1.3 数据准备

| 数据类型 | 说明 | 示例路径 |
|---------|------|---------|
| RT参考序列 | 已知Retron RT的fasta文件 | `/path/to/reference/*.faa` |
| 蛋白序列 | 目标基因组的.faa文件 | `/path/to/faa/*.faa` |
| 基因组序列 | 对应的.fasta文件 | `/path/to/fasta/*.fasta` |
| antiSMASH结果 | 用于提取邻近基因注释 | `/path/to/antismash/` |

---

## 二、配置文件设置

### 2.1 创建配置文件

```bash
cd /home/teng/claude_code/retron/pipeline_v2/retron_scripts
vim my_config.yaml
```

### 2.2 配置文件模板

```yaml
# ===== 路径配置 (必须修改) =====
paths:
  query_dir: /你的路径/reference          # RT参考序列
  faa_dir: /你的路径/faa_files            # 蛋白序列目录
  fasta_dir: /你的路径/fasta_files        # 基因组序列目录
  antismash_dir: /你的路径/antismash      # antiSMASH结果
  output_dir: /你的路径/output            # 输出目录

# ===== 搜索参数 =====
search:
  min_identity: 25    # 最小序列一致性 (%)
  min_coverage: 40    # 最小覆盖度 (%)

# ===== RNA筛选参数 =====
rna_filter:
  min_mfe: -15.0      # 最小自由能阈值 (kcal/mol)
  window_size: 150    # 滑动窗口大小 (bp)
  require_branch_g: false

# ===== 邻近蛋白提取参数 (Mestre方法) =====
neighbors:
  distance: 30        # 上下游提取距离 (kb)

# ===== MMseqs2聚类参数 =====
clustering:
  min_seq_id: 0.3     # 最小序列一致性 (30%)
  coverage: 0.8       # 最小覆盖度 (80%)

# ===== Phyvalue统计参数 =====
association:
  n_permutations: 1000    # 置换检验次数
  min_occurrence: 5       # 最小出现次数
  min_phyvalue: 2.0       # 最小Phyvalue阈值

# ===== 流程控制 =====
pipeline:
  start_step: 1
  end_step: 9
```

---

## 三、运行方案

### 方案A：分段运行（推荐，适应MMseqs2单独环境）

由于MMseqs2需要在单独环境运行，推荐分三段执行：

```bash
cd /home/teng/claude_code/retron/pipeline_v2/retron_scripts

# ===== 第一段：Step 1-6 (主环境) =====
# 运行RT搜索到邻近蛋白提取
python main_pipeline.py -c my_config.yaml --end-step 6

# ===== 第二段：Step 7 (MMseqs2环境) =====
conda activate mmseqs_env

# 单独运行MMseqs2聚类
python step07_mmseqs_cluster.py \
  -i ./output/06_neighbors \
  -o ./output/07_clustering \
  --min-seq-id 0.3 \
  --coverage 0.8 \
  --threads 8

# ===== 第三段：Step 8-9 (主环境) =====
conda deactivate  # 或 conda activate 主环境名

# 继续运行Phyvalue分析和报告生成
python main_pipeline.py -c my_config.yaml --start-step 8
```

### 方案B：跳过Step 1（使用已有搜索结果）

```bash
# 第一段：Step 2-6
python main_pipeline.py \
  --search-results /path/to/existing/complete_results.tsv \
  --faa-dir /path/to/faa \
  --fasta-dir /path/to/fasta \
  --antismash-dir /path/to/antismash \
  --output ./output \
  --start-step 2 \
  --end-step 6

# 第二段：MMseqs2聚类 (切换环境)
conda activate mmseqs_env
python step07_mmseqs_cluster.py -i ./output/06_neighbors -o ./output/07_clustering

# 第三段：统计分析和报告
conda deactivate
python main_pipeline.py -c my_config.yaml --start-step 8
```

### 方案C：完整分步运行（调试用）

```bash
# ===== Step 1: RT搜索 =====
python step01_search.py \
  -q /path/to/reference \
  -d /path/to/faa \
  -a /path/to/antismash \
  -o ./01_search \
  -t 8

# ===== Step 2: Motif过滤 =====
python step02_motif_filter.py \
  -i ./01_search/reports/complete_results.tsv \
  -f /path/to/faa \
  -o ./02_motif_filtered

# ===== Step 3: RT分类 =====
python step03_classify_rt.py \
  -i ./02_motif_filtered/motif_filtered.tsv \
  -o ./03_classified

# ===== Step 4: 上游序列提取 =====
python step04_extract_flanking.py \
  -i ./03_classified/retron_candidates.tsv \
  -f /path/to/fasta \
  -o ./04_flanking

# ===== Step 5: RNA结构筛选 =====
python step05_rna_filter.py \
  -i ./04_flanking/flanking_sequences.fasta \
  -o ./05_rna_filtered \
  --window 150 \
  --min-mfe -15.0

# ===== Step 6: 全量邻近蛋白提取 (±30kb) =====
python step06_extract_neighbors.py \
  -i ./05_rna_filtered/filter_results.tsv \
  -a /path/to/antismash \
  -o ./06_neighbors \
  --distance 30

# ===== Step 7: MMseqs2聚类 (切换到mmseqs_env环境) =====
conda activate mmseqs_env

python step07_mmseqs_cluster.py \
  -i ./06_neighbors \
  -o ./07_clustering \
  --min-seq-id 0.3 \
  --coverage 0.8 \
  --threads 8

conda deactivate

# ===== Step 8: Phyvalue统计关联分析 =====
python step08_phyvalue_analysis.py \
  -i ./07_clustering \
  -o ./08_association \
  --n-permutations 1000 \
  --min-occurrence 5 \
  --min-phyvalue 2.0

# ===== Step 9: 综合报告与类型分类 =====
python step09_final_report.py \
  -o ./09_report \
  --rt-info ./06_neighbors/rt_info.tsv \
  --motif ./02_motif_filtered/motif_filtered.tsv \
  --classified ./03_classified/retron_candidates.tsv \
  --rna ./05_rna_filtered/filter_results.tsv \
  --matrix ./07_clustering/rt_cluster_matrix.tsv \
  --cluster-stats ./08_association/phyvalue_analysis.tsv \
  --neighbor-matrix ./06_neighbors/neighbor_matrix.tsv
```

---

## 四、MMseqs2 单独环境运行详解 (Step 7)

### 4.1 标准两步流程

```bash
# ===== 在主环境运行 Step 1-6 =====
python main_pipeline.py -c my_config.yaml --end-step 6

# 检查Step 6输出
ls -la ./output/06_neighbors/
# 应该看到: all_neighbors.faa, neighbor_matrix.tsv, rt_info.tsv

# ===== 切换到MMseqs2环境运行 Step 7 =====
conda activate mmseqs_env

# 运行聚类
python step07_mmseqs_cluster.py \
  -i ./output/06_neighbors \
  -o ./output/07_clustering \
  --min-seq-id 0.3 \
  --coverage 0.8 \
  --threads 8

# 检查输出
ls -la ./output/07_clustering/
# 应该看到: rt_cluster_matrix.tsv, cluster_members.tsv, cluster_stats.tsv

# ===== 回到主环境继续 =====
conda deactivate
python main_pipeline.py -c my_config.yaml --start-step 8
```

### 4.2 使用已有聚类结果

如果你已经有MMseqs2聚类结果（TSV格式，两列：代表序列ID，成员序列ID）：

```bash
python step07_mmseqs_cluster.py \
  -i ./06_neighbors \
  -o ./07_clustering \
  --existing-clusters /path/to/existing_cluster.tsv
```

### 4.3 MMseqs2参数说明

| 参数 | 默认值 | 说明 |
|------|-------|------|
| `--min-seq-id` | 0.3 | 最小序列一致性 (30%) |
| `--coverage` | 0.8 | 最小覆盖度 (80%) |
| `--cov-mode` | 0 | 覆盖度模式: 0=双向, 1=目标, 2=查询 |
| `--threads` | 8 | 并行线程数 |

---

## 五、Phyvalue 统计分析说明 (Step 8)

### 5.1 核心算法

基于 Mestre et al., 2020 的方法：

1. **构建共现矩阵**: RT × 蛋白簇 的二值矩阵 (0/1)
2. **计算观察频率**: 每个簇在RT邻近出现的次数
3. **随机置换检验**: 打乱矩阵1000次，计算背景期望
4. **计算Phyvalue**: Phyvalue = 观察值 / 期望值
5. **筛选显著关联**: Phyvalue > 2 或 z-score > 4

### 5.2 参数说明

| 参数 | 默认值 | 说明 |
|------|-------|------|
| `--n-permutations` | 1000 | 置换检验次数 (越多越准确，但更慢) |
| `--min-occurrence` | 5 | 最小出现次数阈值 |
| `--min-phyvalue` | 2.0 | 最小Phyvalue阈值 |
| `--no-multi-genome-filter` | False | 设置后不要求在多个基因组中出现 |

### 5.3 输出文件

```
08_association/
├── cooccurrence_frequency.tsv    # 共现频率统计
├── phyvalue_analysis.tsv         # ★ 完整Phyvalue分析结果
├── significant_clusters.tsv      # ★ 显著关联的蛋白簇
├── association_scores.tsv        # 综合关联评分
├── high_association_clusters.tsv # 高关联蛋白簇
└── analysis_stats.txt            # 分析统计报告
```

---

## 六、Retron类型分类说明 (Step 9)

### 6.1 类型定义 (基于 Mestre et al., 2020 Table 1)

| 类型 | 标记关键词 | 效应蛋白类型 | 描述 |
|------|-----------|-------------|------|
| Type I | ATPase, AAA, P-loop, Toprim | ATPase | ATPase/Toprim伴随蛋白 |
| Type II | NDT, Nucleoside deoxyribosyltransferase | NDT | 核苷脱氧核糖基转移酶 |
| Type III | PRTase, Phosphoribosyltransferase | PRTase | 磷酸核糖基转移酶 |
| Type IV | HNH, endonuclease, homing | HNH | HNH核酸内切酶 |
| Type V | DUF4297, DUF | DUF4297 | DUF4297结构域蛋白 |
| Type VI | transmembrane, 2TM, membrane | 2TM | 双跨膜蛋白 |
| Type VII | RNase H, DUF3800, RNaseH | RNaseH | RNase H2-like蛋白 |
| Type VIII | OLD, RecB, nuclease, exonuclease | OLD | OLD/RecB核酸酶 |
| Type IX | HEPN, RNase, abortive infection | HEPN | HEPN结构域RNase |
| Type X | cold shock, CspA, CSD | ColdShock | 冷休克蛋白 |
| Type XI | ribosyltransferase, ADP-ribosyl | Ribosyltransferase | ADP核糖基转移酶 |
| Type XII | toxin, MazF, RelE, ParE | TA | 毒素-抗毒素系统 |
| Type XIII | zinc finger, SWIM, DNA-binding | DNA-binding | DNA结合蛋白 |

### 6.2 置信度评分标准

| 维度 | 分值 | 条件 |
|------|------|------|
| **Motif** | 30分 | NAXXH + VTG 都存在 |
| | 20分 | 仅NAXXH |
| | 15分 | 仅VTG |
| **RNA结构** | 15分 | 茎长度 ≥20bp |
| | 10分 | 茎长度 ≥15bp |
| | 5分 | 有分支G (Branch G) |
| | 5分 | MFE < -25 kcal/mol |
| **关联蛋白** | 30分 | ≥3个显著关联蛋白簇 |
| | 20分 | 1-2个显著关联蛋白簇 |
| **类型预测** | 15分 | 高置信度类型预测 |
| | 10分 | 中等置信度类型预测 |

**置信度等级**:
- **HIGH**: ≥70分
- **MEDIUM**: 45-69分
- **LOW**: 25-44分
- **VERY_LOW**: <25分

---

## 七、输出文件说明

```
output/
├── 01_search/
│   └── reports/complete_results.tsv    # 原始搜索结果 (已去重)
├── 02_motif_filtered/
│   ├── motif_filtered.tsv              # ★ Motif过滤后的候选
│   └── high_confidence.tsv             # NAXXH+VTG都有的RT
├── 03_classified/
│   ├── retron_candidates.tsv           # ★ Retron候选
│   └── excluded_rt.tsv                 # 被排除的RT
├── 04_flanking/
│   └── flanking_sequences.fasta        # 上游序列
├── 05_rna_filtered/
│   ├── filter_results.tsv              # ★ RNA筛选结果
│   └── passed_candidates.fasta         # 通过筛选的序列
├── 06_neighbors/
│   ├── all_neighbors.faa               # ★ 所有邻近蛋白序列 (用于聚类)
│   ├── neighbor_matrix.tsv             # RT-邻近基因对应关系
│   ├── rt_info.tsv                     # RT信息汇总
│   └── extraction_stats.txt            # 提取统计
├── 07_clustering/
│   ├── clusters_rep.faa                # 聚类代表序列
│   ├── cluster_members.tsv             # 聚类成员关系
│   ├── rt_cluster_matrix.tsv           # ★ RT-蛋白簇共现矩阵
│   ├── cluster_stats.tsv               # 蛋白簇统计
│   └── high_frequency_clusters.tsv     # 高频蛋白簇
├── 08_association/
│   ├── phyvalue_analysis.tsv           # ★ Phyvalue分析结果
│   ├── significant_clusters.tsv        # ★ 显著关联蛋白簇
│   ├── association_scores.tsv          # 综合关联评分
│   └── analysis_stats.txt              # 分析统计
└── 09_report/
    ├── final_candidates.tsv            # ★★ 最终结果 (含评分和类型)
    ├── high_confidence_candidates.tsv  # ★★ 高置信度候选
    ├── candidates_Type_*.tsv           # 按类型分组的候选
    ├── classification_summary.tsv      # 类型分布汇总
    └── summary_report.md               # Markdown格式总结报告
```

---

## 八、小样本测试

### 8.1 测试数据准备

```bash
# 创建测试子集
mkdir -p test_data/{faa,fasta}

# 选择包含已知Retron的基因组 (如 Myxococcus xanthus)
cp /path/to/faa/GCF_000012685*.faa test_data/faa/
cp /path/to/fasta/GCF_000012685*.fasta test_data/fasta/
```

### 8.2 测试运行

```bash
# 创建测试配置
cp my_config.yaml test_config.yaml
# 修改路径指向test_data

# 运行Step 1-6
python main_pipeline.py -c test_config.yaml --end-step 6 -v

# 运行Step 7 (MMseqs2环境)
conda activate mmseqs_env
python step07_mmseqs_cluster.py -i ./test_output/06_neighbors -o ./test_output/07_clustering -v
conda deactivate

# 运行Step 8-9
python main_pipeline.py -c test_config.yaml --start-step 8 -v

# 查看结果
cat ./test_output/09_report/summary_report.md
```

### 8.3 验证检查点

| 步骤 | 检查项 |
|------|--------|
| Step 1 | 日志显示位置去重数量 |
| Step 2 | 高置信度(NAXXH+VTG)数量合理 |
| Step 5 | 能检出已知Retron的ncRNA结构 |
| Step 6 | 每个RT平均提取到合理数量的邻近蛋白 |
| Step 7 | 生成了rt_cluster_matrix.tsv |
| Step 8 | 筛选出显著关联蛋白簇 |
| Step 9 | 高置信度候选包含已知Retron |

---

## 九、常见问题

### Q1: Step 7 MMseqs2报错"命令未找到"

```bash
# 确保在正确的环境中
conda activate mmseqs_env
which mmseqs  # 应该显示路径
```

### Q2: Step 8 Phyvalue全是0或inf

检查Step 7输出：
```bash
# 查看共现矩阵是否正确生成
head ./07_clustering/rt_cluster_matrix.tsv

# 检查是否有蛋白簇
wc -l ./07_clustering/cluster_stats.tsv
```

### Q3: 如何只看特定类型的候选？

```bash
# 查看Type IV (HNH核酸内切酶) 候选
cat ./09_report/candidates_Type_IV.tsv

# 或筛选
grep "Type_IV" ./09_report/final_candidates.tsv
```

### Q4: 如何调整Phyvalue阈值？

```bash
# 更严格 (减少假阳性)
python step08_phyvalue_analysis.py -i ./07_clustering -o ./08_association \
  --min-phyvalue 3.0 --min-occurrence 10

# 更宽松 (发现更多)
python step08_phyvalue_analysis.py -i ./07_clustering -o ./08_association \
  --min-phyvalue 1.5 --min-occurrence 3
```

### Q5: 如何获取关联蛋白的功能注释？

目前类型分类基于GBK文件中的product注释。如需更准确的注释：

```bash
# 提取显著关联簇的代表序列
# 然后进行InterProScan或HHpred分析
```

---

## 十、后续分析建议

1. **高置信度候选** (≥70分):
   - 直接进行实验验证
   - msDNA检测、RT-PCR、噬菌体攻毒实验

2. **有类型预测的候选**:
   - 参考对应类型的已知Retron设计验证实验
   - 如Type IV (HNH) → 检测DNA切割活性

3. **Unknown类型候选**:
   - 可能是新型Retron
   - 对关联蛋白进行HHpred结构预测

4. **系统发育分析**:
   - 对高置信度RT构建进化树
   - 分析Retron类型的进化关系

5. **扩展搜索**:
   - 使用高置信度ncRNA构建CM模型
   - 对更多基因组进行搜索
