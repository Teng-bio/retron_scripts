# Retron系统挖掘流程 v2.0 - 完整运行方案

## 方法论说明

本流程基于 **Mestre et al., 2020 NAR** 的统计关联分析方法：

| 方法 | 核心思路 | 工具依赖 |
|------|---------|---------|
| **Mestre (本流程)** | RT → 系统发育分类 → 提取±30kb邻近蛋白 → MMseqs2聚类 → Phyvalue统计 → 类型分类 → ncRNA共变验证 | MAFFT, FastTree, MMseqs2, R-scape |
| Millman (旧方法) | RT → 检测防御岛 → DefenseFinder验证 | DefenseFinder |

**优势**：
- RT分类采用系统发育分析（而非不可靠的文本匹配）
- ncRNA验证采用多序列共变分析（适用于高GC生物，如Myxobacteria ~70% GC）
- 无监督聚类可发现新型关联蛋白

---

## 目录结构

```
/home/teng/claude_code/retron/pipeline_v2/retron_scripts/
├── main_pipeline.py               # 主控脚本
├── step01_search.py               # RT同源物搜索 (含位置去重)
├── step02_motif_filter.py         # Motif过滤 (NAXXH/VTG)
├── step03_phylogeny.py            # ★ RT系统发育分析 (MAFFT+FastTree)
├── step04_extract_neighbors.py    # ★ 全量邻近蛋白提取 (±30kb)
├── step05_mmseqs_cluster.py       # ★ MMseqs2聚类与共现矩阵
├── step06_phyvalue_analysis.py    # ★ Phyvalue统计关联分析
├── step07_final_report.py         # ★ Retron类型分类与报告
├── step08_ncrna_validation.py     # ★ ncRNA共变验证 (可选)
├── config.yaml                    # 配置文件模板
└── 运行方案.md                     # 本文档
```

---

## 一、环境准备

### 1.1 主环境依赖

```bash
# Python包
pip install biopython pandas numpy pyyaml tqdm psutil

# 核心外部工具
conda install -c bioconda diamond mafft fasttree

# ncRNA验证工具 (Step 8)
conda install -c bioconda viennarna r-scape

# 检查工具
which diamond mafft fasttree RNAalifold R-scape
```

### 1.2 MMseqs2 单独环境 (必须)

由于MMseqs2环境配置问题，需要在单独的conda环境中运行：

```bash
# 创建MMseqs2环境
conda create -n mmseqs_env python=3.9
conda activate mmseqs_env
conda install -c bioconda mmseqs2

# 验证安装
mmseqs --help
```

### 1.3 数据准备

| 数据类型 | 说明 | 示例路径 |
|---------|------|---------|
| RT参考序列 | 已知Retron RT的fasta文件 | `/path/to/reference/*.faa` |
| 蛋白序列 | 目标基因组的.faa文件 | `/path/to/faa/*.faa` |
| 基因组序列 | 对应的.fasta文件 | `/path/to/fasta/*.fasta` |
| antiSMASH结果 | 用于提取邻近基因注释 | `/path/to/antismash/` |

---

## 二、配置文件设置

### 2.1 创建配置文件

```bash
cd /home/teng/claude_code/retron/pipeline_v2/retron_scripts
cp config.yaml my_config.yaml
vim my_config.yaml
```

### 2.2 关键配置项

```yaml
# ===== 路径配置 (必须修改) =====
paths:
  query_dir: /你的路径/reference          # RT参考序列
  faa_dir: /你的路径/faa_files            # 蛋白序列目录
  fasta_dir: /你的路径/fasta_files        # 基因组序列目录
  antismash_dir: /你的路径/antismash      # antiSMASH结果
  output_dir: /你的路径/output            # 输出目录

# ===== 搜索参数 (Step 1) =====
search:
  min_identity: 25    # 最小序列一致性 (%)
  min_coverage: 40    # 最小覆盖度 (%)

# ===== 系统发育参数 (Step 3) =====
phylogeny:
  distance_threshold: 0.5   # 系统发育距离阈值

# ===== 邻近蛋白提取参数 (Step 4) =====
neighbors:
  distance: 20        # 上下游提取距离 (kb)

# ===== MMseqs2聚类参数 (Step 5) =====
clustering:
  min_seq_id: 0.3     # 最小序列一致性 (30%)
  coverage: 0.8       # 最小覆盖度 (80%)

# ===== Phyvalue统计参数 (Step 6) =====
association:
  n_permutations: 1000    # 置换检验次数
  min_occurrence: 5       # 最小出现次数
  min_phyvalue: 2.0       # 最小Phyvalue阈值

# ===== ncRNA验证参数 (Step 8) =====
ncrna:
  upstream: 600           # 上游提取长度 (bp)
  min_group_size: 3       # 最小分组大小

# ===== 流程控制 =====
pipeline:
  start_step: 1
  end_step: 8
  skip_ncrna: false       # 是否跳过ncRNA验证
```

---

## 三、运行方案

### 方案A：分段运行（推荐，适应MMseqs2单独环境）

```bash
cd /home/teng/claude_code/retron/pipeline_v2/retron_scripts

# ===== 第一段：Step 1-4 (主环境) =====
# 运行RT搜索、Motif过滤、系统发育分析、邻近蛋白提取
python main_pipeline.py -c my_config.yaml --end-step 4

# ===== 第二段：Step 5 (MMseqs2环境) =====
conda activate mmseqs_env

python step05_mmseqs_cluster.py \
  -i ./output/04_neighbors \
  -o ./output/05_clustering \
  --min-seq-id 0.3 \
  --coverage 0.8 \
  --threads 8

conda deactivate

# ===== 第三段：Step 6-8 (主环境) =====
# 继续运行Phyvalue分析、报告生成、ncRNA验证
python main_pipeline.py -c my_config.yaml --start-step 6
```

### 方案B：跳过ncRNA验证

```bash
# 如果不需要ncRNA共变验证（节省时间）
python main_pipeline.py -c my_config.yaml --skip-ncrna

# 或在配置文件中设置
# pipeline:
#   skip_ncrna: true
```

### 方案C：完整分步运行（调试用）

```bash
# ===== Step 1: RT搜索 =====
python step01_search.py \
  -q /path/to/reference \
  -d /path/to/faa \
  -a /path/to/antismash \
  -o ./01_search \
  -t 8

# ===== Step 2: Motif过滤 =====
python step02_motif_filter.py \
  -i ./01_search/reports/complete_results.tsv \
  -f /path/to/faa \
  -o ./02_motif_filtered

# ===== Step 3: RT系统发育分析 (NEW) =====
python step03_phylogeny.py \
  -i ./02_motif_filtered/motif_filtered.tsv \
  -f /path/to/faa \
  -o ./03_phylogeny \
  --distance-threshold 0.5 \
  --threads 8

# ===== Step 4: 全量邻近蛋白提取 (±30kb) =====
python step04_extract_neighbors.py \
  -i ./03_phylogeny/retron_candidates.tsv \
  -a /path/to/antismash \
  -o ./04_neighbors \
  --distance 20

# ===== Step 5: MMseqs2聚类 (切换到mmseqs_env环境) =====
conda activate mmseqs_env

python step05_mmseqs_cluster.py \
  -i ./04_neighbors \
  -o ./05_clustering \
  --min-seq-id 0.3 \
  --coverage 0.8 \
  --threads 8

conda deactivate

# ===== Step 6: Phyvalue统计关联分析 =====
python step06_phyvalue_analysis.py \
  -i ./05_clustering \
  -o ./06_association \
  --n-permutations 1000 \
  --min-occurrence 5 \
  --min-phyvalue 2.0

# ===== Step 7: 综合报告与类型分类 =====
python step07_final_report.py \
  -o ./07_report \
  --rt-info ./04_neighbors/rt_info.tsv \
  --motif ./02_motif_filtered/motif_filtered.tsv \
  --phylogeny ./03_phylogeny/retron_candidates.tsv \
  --matrix ./05_clustering/rt_cluster_matrix.tsv \
  --cluster-stats ./06_association/phyvalue_analysis.tsv \
  --neighbor-matrix ./04_neighbors/neighbor_matrix.tsv

# ===== Step 8: ncRNA共变验证 (可选) =====
python step08_ncrna_validation.py \
  -i ./07_report/final_candidates.tsv \
  -f /path/to/fasta \
  -o ./08_ncrna \
  --phylogeny ./03_phylogeny/retron_candidates.tsv \
  --upstream 600 \
  --min-group-size 3 \
  --threads 8
```

---

## 四、新步骤详解

### 4.1 Step 3: RT系统发育分析

**目的**：通过系统发育树计算候选RT到已知Retron参考序列的距离，进行置信度标注

**重要说明**：
- 由于缺乏可靠的Group II Intron/DGR/CRISPR RT参考序列，本流程**不进行这些RT类型的分类标注**
- 只使用5条已知Retron RT参考序列进行距离计算
- 基于距离远近进行置信度标注，由用户自行决定如何处理

**方法**：
1. 收集候选RT序列 + 5条Retron参考序列
2. MAFFT多序列比对
3. FastTree构建系统发育树
4. 计算到各Retron参考序列的距离，进行置信度标注

**参考序列**（从 reference_dir 加载，当前12条）：
| 序列ID | 来源 |
|--------|------|
| Ec67_RT_Coli | E. coli Retron-Ec67 |
| Ec86_RT_Coli | E. coli Retron-Ec86 |
| Ec73_RT_Coli | E. coli Retron-Ec73 |
| Ec83_RT_Coli | E. coli Retron-Ec83 |
| Ec48_RT_Coli | E. coli Retron-Ec48 |
| Ec107_RT_Coli | E. coli Retron-Ec107 |
| Ec78_RT_Coli | E. coli Retron-Ec78 |
| MX65_RT_Myxo | M. xanthus Retron-Mx65 |
| ML162_RT_Melittangium | Melittangium lichenicola |
| Sa163_RT_Stigmatella | Stigmatella aurantiaca |
| Vc95_RT_Vibrio | Vibrio cholerae |
| Se72_RT_Salmonella | Salmonella heidelberg |

**输出字段说明**：
| 字段 | 说明 |
|------|------|
| closest_retron_ref | 最近的Retron参考序列 |
| distance_to_closest_ref | 到最近参考的系统发育距离 |
| phylo_confidence | 置信度等级 (high/medium/low/very_low) |
| recommended_action | 建议操作 (keep/review) |
| distance_to_Ec67/Ec86/Mx65/Mx162/Stig | 到各参考序列的距离 |

**置信度等级说明**：
- **high**: 距离 < 0.3，高置信度Retron
- **medium**: 0.3 ≤ 距离 < threshold，中等置信度
- **low**: threshold ≤ 距离 < threshold×1.5，低置信度
- **very_low**: 距离 ≥ threshold×1.5，可能不是Retron

**建议操作**：
- **keep**: high/medium置信度，建议保留
- **review**: low/very_low置信度，需要人工检查

**输出**：
```
03_phylogeny/
├── retron_candidates.tsv          # ★ 所有候选（含距离和置信度标注）
├── rt_classification_summary.tsv  # 置信度汇总统计
├── rt_alignment.fasta             # MAFFT比对结果
├── rt_tree.nwk                    # 系统发育树
└── phylogeny_stats.txt            # 分析统计
```

### 4.2 Step 8: ncRNA共变验证

**目的**：使用多序列比对的共变分析验证ncRNA结构（适用于高GC生物）

**为什么需要**：
- 单序列RNAfold对高GC生物（如Myxobacteria ~70% GC）产生大量假阳性
- 共变分析通过统计学方法验证碱基配对

**方法**：
1. 按RT系统发育分组候选
2. 提取各组RT上游序列
3. MAFFT多序列比对
4. RNAalifold预测共识结构
5. R-scape共变分析（检测统计显著配对）

**输出**：
```
08_ncrna/
├── covariance_results.tsv      # 共变分析汇总
├── validated_candidates.tsv    # ★ 通过验证的候选
├── validation_stats.txt        # 验证统计
└── group_*/                    # 每组分析结果
    ├── alignment.fasta         # FASTA比对
    ├── alignment.stk           # Stockholm格式（含结构）
    └── (R-scape输出)
```

---

## 五、输出文件说明

```
output/
├── 01_search/
│   └── reports/complete_results.tsv    # 原始搜索结果 (已去重)
├── 02_motif_filtered/
│   ├── motif_filtered.tsv              # ★ Motif过滤后的候选
│   └── high_confidence.tsv             # NAXXH+VTG都有的RT
├── 03_phylogeny/                       # ★ 系统发育距离标注
│   ├── retron_candidates.tsv           # ★ 所有候选 (含距离和置信度标注)
│   ├── rt_classification_summary.tsv   # 置信度汇总统计
│   ├── rt_alignment.fasta              # MAFFT比对
│   ├── rt_tree.nwk                     # 系统发育树
│   └── phylogeny_stats.txt             # 分析统计
├── 04_neighbors/
│   ├── all_neighbors.faa               # ★ 所有邻近蛋白序列 (用于聚类)
│   ├── neighbor_matrix.tsv             # RT-邻近基因对应关系
│   └── rt_info.tsv                     # RT信息汇总
├── 05_clustering/
│   ├── rt_cluster_matrix.tsv           # ★ RT-蛋白簇共现矩阵
│   ├── cluster_members.tsv             # 聚类成员关系
│   └── cluster_stats.tsv               # 蛋白簇统计
├── 06_association/
│   ├── phyvalue_analysis.tsv           # ★ Phyvalue分析结果
│   └── significant_clusters.tsv        # ★ 显著关联蛋白簇
├── 07_report/
│   ├── final_candidates.tsv            # ★★ 最终结果 (含评分和类型)
│   └── high_confidence_candidates.tsv  # ★★ 高置信度候选
└── 08_ncrna/                           # ★ 新增 (可选)
    ├── covariance_results.tsv          # 共变分析结果
    ├── validated_candidates.tsv        # ★ 通过ncRNA验证的候选
    └── group_*/                        # 每组分析详情
```

---

## 六、Retron类型分类说明 (Step 7)

### 6.1 类型定义 (基于 Mestre et al., 2020 Table 1)

| 类型 | 标记关键词 | 效应蛋白类型 | 描述 |
|------|-----------|-------------|------|
| Type I | ATPase, AAA, P-loop, Toprim | ATPase | ATPase/Toprim伴随蛋白 |
| Type II | NDT, Nucleoside deoxyribosyltransferase | NDT | 核苷脱氧核糖基转移酶 |
| Type III | PRTase, Phosphoribosyltransferase | PRTase | 磷酸核糖基转移酶 |
| Type IV | HNH, endonuclease, homing | HNH | HNH核酸内切酶 |
| Type V | DUF4297, DUF | DUF4297 | DUF4297结构域蛋白 |
| Type VI | transmembrane, 2TM, membrane | 2TM | 双跨膜蛋白 |
| Type VII | RNase H, DUF3800, RNaseH | RNaseH | RNase H2-like蛋白 |
| Type VIII | OLD, RecB, nuclease, exonuclease | OLD | OLD/RecB核酸酶 |
| Type IX | HEPN, RNase, abortive infection | HEPN | HEPN结构域RNase |
| Type X | cold shock, CspA, CSD | ColdShock | 冷休克蛋白 |
| Type XI | ribosyltransferase, ADP-ribosyl | Ribosyltransferase | ADP核糖基转移酶 |
| Type XII | toxin, MazF, RelE, ParE | TA | 毒素-抗毒素系统 |
| Type XIII | zinc finger, SWIM, DNA-binding | DNA-binding | DNA结合蛋白 |

---

## 七、常见问题

### Q1: Step 3 MAFFT或FastTree报错

```bash
# 确保工具已安装
which mafft fasttree

# 如果未安装
conda install -c bioconda mafft fasttree
```

### Q2: Step 5 MMseqs2报错"命令未找到"

```bash
# 确保在正确的环境中
conda activate mmseqs_env
which mmseqs  # 应该显示路径
```

### Q3: Step 8 R-scape未安装

```bash
# R-scape是可选的，即使未安装Step 8也能运行
# 只是不会有共变分析结果

# 安装R-scape
conda install -c bioconda r-scape
```

### Q4: 高GC生物的ncRNA验证

对于高GC生物（如Myxobacteria），**必须使用Step 8的共变分析**，而非单序列折叠。

```bash
# 确保不跳过Step 8
python main_pipeline.py -c my_config.yaml  # 默认包含Step 8
```

### Q5: 如何查看系统发育树？

```bash
# 使用FigTree或在线工具查看
# 文件位置: 03_phylogeny/rt_tree.nwk

# 在线工具: https://itol.embl.de/
```

### Q6: 如何调整系统发育距离阈值？

```bash
# 更严格 (更高置信度标准，medium范围更窄)
python step03_phylogeny.py -i input.tsv -f /path/to/faa -o 03_phylogeny \
  --distance-threshold 0.3

# 更宽松 (允许更远的距离仍被标为medium置信度)
python step03_phylogeny.py -i input.tsv -f /path/to/faa -o 03_phylogeny \
  --distance-threshold 0.7
```

---

## 八、小样本测试

```bash
# 创建测试子集
mkdir -p test_data/{faa,fasta}

# 选择包含已知Retron的基因组
cp /path/to/faa/GCF_000012685*.faa test_data/faa/
cp /path/to/fasta/GCF_000012685*.fasta test_data/fasta/

# 创建测试配置
cp my_config.yaml test_config.yaml
# 修改路径指向test_data

# 运行Step 1-4
python main_pipeline.py -c test_config.yaml --end-step 4 -v

# 运行Step 5 (MMseqs2环境)
conda activate mmseqs_env
python step05_mmseqs_cluster.py -i ./test_output/04_neighbors -o ./test_output/05_clustering -v
conda deactivate

# 运行Step 6-8
python main_pipeline.py -c test_config.yaml --start-step 6 -v

# 查看结果
cat ./test_output/07_report/final_candidates.tsv
cat ./test_output/08_ncrna/covariance_results.tsv
```

---

## 九、验证检查点

| 步骤 | 检查项 |
|------|--------|
| Step 1 | 日志显示位置去重数量 |
| Step 2 | 高置信度(NAXXH+VTG)数量合理 |
| Step 3 | rt_tree.nwk生成，retron_candidates.tsv含置信度标注 |
| Step 4 | 每个RT平均提取到合理数量的邻近蛋白 |
| Step 5 | rt_cluster_matrix.tsv生成 |
| Step 6 | 筛选出显著关联蛋白簇 |
| Step 7 | final_candidates.tsv包含类型预测 |
| Step 8 | covariance_results.tsv显示成功分析的组数 |

---

## 十、后续分析建议

1. **高置信度候选**:
   - 直接进行实验验证
   - msDNA检测、RT-PCR、噬菌体攻毒实验

2. **ncRNA验证通过的候选** (Step 8):
   - 共变配对≥2表示结构可靠
   - 可直接用于引物设计

3. **系统发育树分析**:
   - 用03_phylogeny/rt_tree.nwk分析RT进化关系
   - 在FigTree或iTOL中可视化

4. **Unknown类型候选**:
   - 可能是新型Retron
   - 对关联蛋白进行HHpred结构预测
